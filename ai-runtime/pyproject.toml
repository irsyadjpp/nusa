[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nusa-ai-runtime"
version = "1.0.0"
description = "NUSA AI Runtime - LangGraph orchestration service"
requires-python = ">=3.11"
